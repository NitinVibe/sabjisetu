# PostgreSQL connection
DATABASE_URL=postgresql://postgres:password@localhost:5432/sabzisetu

# Admin login bootstrap (used only to create the first admin user if it does not exist)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-to-a-strong-password
ADMIN_DISPLAY_NAME=Administrator

# Server
PORT=4000

# Production frontend origin (configure CORS before public deployment)
FRONTEND_URL=http://localhost:5173
